{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "Oracle Bots Custom Component Package",
  "main": "main.js",
  "scripts": {
    "{{sdkBin}}": "node $(npm bin)/{{sdkBin}}",
    "help": "npm run {{sdkBin}} -- --help",
    "prepack": "npm run {{sdkBin}} -- pack --dry-run",
    "start": "npm run {{sdkBin}} -- service ."
  },
  "repository": {},
  "dependencies": {},
  "devDependencies": {
    "{{sdkName}}": "^{{sdkVersion}}",
    "express": "^4.16.4"
  }
}
